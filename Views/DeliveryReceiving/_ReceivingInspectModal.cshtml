@page
@model practice_for_wms.Views.DeliveryReceiving._ReceivingInspectModalModel
@{
}
<div id="receiveModal"
     class="fixed inset-0 bg-black/50 hidden flex items-center justify-center z-50">

    <div class="bg-white w-[900px] rounded-2xl p-8 max-h-[90vh] overflow-y-auto">

        <div class="flex justify-between items-center mb-6">

            <h2 class="text-xl font-semibold">
                Receive Delivery - Company Name
            </h2>

            <button onclick="closeModal('receiveModal')">
                ✕
            </button>

        </div>

        @*Form*@
        <partial name="_FormModal" />

    </div>

</div>