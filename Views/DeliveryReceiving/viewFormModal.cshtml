@page
@model practice_for_wms.Views.DeliveryReceiving.viewFormModalModel
@{
}
<div id="viewReportModal"
     class="fixed inset-0 bg-black/50 hidden flex items-center justify-center z-50">

    <div class="bg-white w-[900px] rounded-2xl p-8 max-h-[90vh] overflow-y-auto">

        <div class="flex justify-between items-center mb-6">

            <h2 class="text-xl font-semibold">
                Inspection Report
            </h2>

            <button onclick="closeModal('viewReportModal')">
                ✕
            </button>

        </div>
        <div class="grid grid-cols-2">
            <div>
                <span class="text-sm text-gray-200">Supplier</span>
            </div>
        </div>
    </div>

</div>